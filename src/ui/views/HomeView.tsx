// ============================================================================
// src/ui/views/HomeView.tsx — STUB (Skeleton phase). Replaced by the view
// agent; keep the contract: no props, reads state via hooks, no own <h2>
// (the Shell header renders the screen title, docs/UI_CONVENTIONS.md).
// ============================================================================
import { copy } from '../copy'

export function HomeView() {
  return <div className="screen screen--home" data-screen={copy.nav.home} />
}
