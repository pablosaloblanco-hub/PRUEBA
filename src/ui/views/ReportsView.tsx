// ============================================================================
// src/ui/views/ReportsView.tsx — STUB (Skeleton phase). Replaced by the view
// agent; keep the contract: no props, reads state via hooks, no own <h2>
// (the Shell header renders the screen title, docs/UI_CONVENTIONS.md).
// ============================================================================
import { copy } from '../copy'

export function ReportsView() {
  return <div className="screen screen--reports" data-screen={copy.nav.reports} />
}
