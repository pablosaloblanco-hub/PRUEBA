// ============================================================================
// src/ui/views/SettingsView.tsx — STUB (Skeleton phase). Replaced by the view
// agent; keep the contract: no props, reads state via hooks, no own <h2>
// (the Shell header renders the screen title, docs/UI_CONVENTIONS.md).
// ============================================================================
import { copy } from '../copy'

export function SettingsView() {
  return <div className="screen screen--settings" data-screen={copy.nav.settings} />
}
