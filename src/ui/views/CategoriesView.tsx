// ============================================================================
// src/ui/views/CategoriesView.tsx — STUB (Skeleton phase). Replaced by the view
// agent; keep the contract: no props, reads state via hooks, no own <h2>
// (the Shell header renders the screen title, docs/UI_CONVENTIONS.md).
// ============================================================================
import { copy } from '../copy'

export function CategoriesView() {
  return <div className="screen screen--categories" data-screen={copy.nav.categories} />
}
